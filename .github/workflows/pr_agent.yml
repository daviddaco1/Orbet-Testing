on:
  pull_request:
    types: [opened, reopened, ready_for_review]
  issue_comment:
jobs:
  pr_agent_job:
    if: ${{ github.event.sender.type != 'Bot' }}
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: write
    name: Run pr agent on every pull request, respond to user comments
    steps:
      - name: PR Agent action step
        id: pragent
        uses: qodo-ai/pr-agent@main
        env:
          OPENAI_KEY: ${{ secrets.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 }}
          GITHUB_TOKEN: ${{ secrets.testing_key }}